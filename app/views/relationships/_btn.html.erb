<% if current_user != user %>
 <% if current_user.followeres?(user) %>
   <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, class: "btn btn-info" %>
 <% else %>
  <%= link_to "フォローする", user_relationships_path(usre.id), method: :past, class: "btn btn-success" %>
 <% end %>
<% end %>